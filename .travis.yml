language: python
python:
  - "3.5"
install: pip install -r requirements.txt
env: SQLALCHEMY_DATABASE_URI = "sqlite:///storage.db" SQLALCHEMY_TRACK_MODIFICATIONS = True SECRET_KEY = "teste123"
before_script:
  - psql -c 'create database test;' -U postgres
deploy:
    provider: heroku
    api_key:
        secure: "seu_token"
script: pytest